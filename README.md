# ByteBazaar
Byte Bazaar is a small electronics shopping website made with Spring Boot and a simple AngularJS frontend.  It has login, product listing, cart and order placement. The app runs with an in-memory H2 database by default, so it can be tested without installing MySQL..
